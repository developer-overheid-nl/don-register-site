/**
 * This file is a fallback.
 * Put the oas url of the api in `[root]/redocly.yaml` and run `ts:oas`.
 */

export type paths = Record<string, unknown>;
export type webhooks = Record<string, unknown>;
export interface components {
  schemas: unknown;
  responses: unknown;
  parameters: unknown;
  requestBodies: unknown;
  headers: unknown;
  pathItems: unknown;
}
export type $defs = Record<string, unknown>;
export type operations = Record<string, unknown>;
