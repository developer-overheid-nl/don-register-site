import {
  type ListboxProps,
  Listbox as RHCListbox,
} from "@rijkshuisstijl-community/components-react";

const Listbox = (props: ListboxProps) => {
  return <RHCListbox {...props} />;
};

export default Listbox;
